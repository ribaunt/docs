---
title: "verifySolution"
sidebarTitle: "verifySolution"
description: "verifySolution(token, nonce, options?), JWT imzalarını, hash kanıtlarını, süre dolumunu ve replay korumasını doğrular. Promise<boolean> döndürür."
---

`verifySolution()`, tarayıcının tüm challenge token'larını doğru şekilde çözüp çözmediğini kontrol etmek için verify endpoint'inizde sunucu tarafında çağrılır. JWT imzasını, token süresini, SHA-256 hash kanıtını kontrol eder ve (varsayılan olarak) token yeniden kullanımını engeller.

## İçe aktarma

```ts
import { verifySolution } from 'ribaunt';
```

## İmza

```ts
function verifySolution(
  token: ChallengeToken | ChallengeToken[],
  nonce: number | string | Array<number | string> | ChallengeSolution | ChallengeSolution[],
  options?: VerifySolutionOptions
): Promise<boolean>
```

## Parametreler

<ParamField path="token" type="ChallengeToken | ChallengeToken[]" required>
  `createChallenge()` tarafından döndürülen orijinal JWT token'ı/token'ları. Challenge endpoint'inizin yayınladığı aynı token'ları iletin.
</ParamField>

<ParamField path="nonce" type="number | string | Array<number | string> | ChallengeSolution | ChallengeSolution[]" required>
  Tarayıcı tarafından gönderilen çözüm(ler). Şunlar olabilir:

  - Tek bir nonce dizesi
  - Nonce dizelerinden oluşan bir dizi
  - Bir `ChallengeSolution` nesnesi `{ nonce: string; hash: string }`
  - `ChallengeSolution` nesnelerinden oluşan bir dizi (widget'ın `solutions` olarak gönderdiği şey)
</ParamField>

<ParamField path="options" type="VerifySolutionOptions">
  Opsiyonel yapılandırma nesnesi. Aşağıdaki seçenekler tablosuna bakın.
</ParamField>

## Seçenekler

| Seçenek | Tür | Varsayılan | Açıklama |
| --- | --- | --- | --- |
| `replayPrevention` | `'local' | 'remote' | 'disabled'` | `'local'` | Token yeniden kullanımının nasıl engelleneceğini kontrol eder |
| `replayStore` | `ReplayStore` | `undefined` | `replayPrevention` `'remote'` olduğunda gereklidir. `consume()` metodunu uygular. |
| `debug` | `boolean` | otomatik (dev'de true) | Doğrulama uyarılarını konsola günlükler |
| `onWarning` | `(warning: VerifyWarning) => void` | `undefined` | Yapılandırılmış uyarı olayları için callback |

## Dönüş değeri

`Promise<boolean>`. Yalnızca TÜM token'ların geçerli çözümleri varsa ve replay kontrollerinden geçerse `true` döndürür. Herhangi bir başarısızlık için hata fırlatmadan `false` döndürür.

## Örnekler

Temel kullanım:

```ts
const { tokens, solutions } = req.body;
const valid = await verifySolution(tokens, solutions);
```

Yapılandırılmış uyarı telemetrisi ile:

```ts
const valid = await verifySolution(tokens, solutions, {
  onWarning: (warning) => {
    // warning.reason: 'invalid-token' | 'expired-token' | 'invalid-solution' | 'replay-detected' | 'configuration-error'
    console.log('captcha warning', warning.reason, warning.message);
  },
});
```

Uzak replay store ile:

```ts
const valid = await verifySolution(tokens, solutions, {
  replayPrevention: 'remote',
  replayStore: {
    consume: async (jti, expiresAt) => {
      // Atomic set-if-not-exists with TTL (e.g. Redis SET NX EX)
      return await redis.set(jti, '1', 'NX', 'EXAT', expiresAt) !== null;
    },
  },
});
```

## Uyarı sebepleri

| Sebep | Açıklama |
| --- | --- |
| `invalid-token` | JWT imzası geçersiz veya bozuk |
| `expired-token` | Challenge TTL'i geçti |
| `invalid-solution` | Nonce geçerli bir hash üretmiyor |
| `replay-detected` | Token zaten tüketildi |
| `configuration-error` | `replayPrevention` `'remote'` ancak `replayStore` sağlanmadı |
