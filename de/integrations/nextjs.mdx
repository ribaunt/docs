---
title: "Ribaunt-CAPTCHA in Next.js (App Router) integrieren"
sidebarTitle: "Next.js"
description: "Fügen Sie Ribaunt-CAPTCHA mit dem React-Wrapper in einer Client-Komponente zu Next.js hinzu. Behandelt App-Router-Setup, SSR-Schutz und Sicherheit von Umgebungsvariablen."
---

Der React-Wrapper von Ribaunt unter `ribaunt/widget-react` übernimmt SSR-Schutz automatisch, indem er die nur im Browser ausführbare Web-Komponente dynamisch auf dem Client importiert. Fügen Sie `'use client'` zu Ihrer Komponentendatei hinzu, importieren Sie `RibauntWidget`, und schon sind Sie startklar — keine zusätzliche Konfiguration erforderlich.

<Warning>
  **Setzen Sie niemals ein `NEXT_PUBLIC_`-Präfix vor `RIBAUNT_SECRET`.**  
  Das würde das Geheimnis in Ihr Client-Bundle einbetten und es jedem Besucher offenlegen. Behalten Sie `RIBAUNT_SECRET` in `.env.local` ohne das `NEXT_PUBLIC_`-Präfix, damit es ausschließlich auf dem Server bleibt.
</Warning>

## Client-Komponenten-Setup

Erstellen Sie eine Client-Komponente und fügen Sie `RibauntWidget` ein. Da der Wrapper das dynamische Importieren intern übernimmt, müssen Sie ihn **nicht** in `next/dynamic` einwickeln.

```tsx
'use client';

import { useRef } from 'react';
import RibauntWidget, { type RibauntWidgetHandle } from 'ribaunt/widget-react';

export default function ContactForm() {
  const widgetRef = useRef<RibauntWidgetHandle>(null);

  return (
    <form>
      <RibauntWidget
        ref={widgetRef}
        challengeEndpoint="/api/captcha/challenge"
        verifyEndpoint="/api/captcha/verify"
        autoVerify={true}
        solveTimeout={15000}
        onVerify={(detail) => console.log('Verified:', detail.solutions)}
        onError={(detail) => console.error('Failed:', detail.error)}
        onReady={(detail) => console.log('Ready:', detail.state)}
      />
      <button type="submit">Submit</button>
    </form>
  );
}
```

<Note>
  **NICHT in `next/dynamic` einwickeln** — der Wrapper `ribaunt/widget-react` übernimmt das dynamische Importieren bereits intern. Ein zusätzlicher `next/dynamic`-Aufruf ist überflüssig und kann zu doppeltem Laden führen.
</Note>

## API-Routen

Die obige Client-Komponente ruft `/api/captcha/challenge` und `/api/captcha/verify` auf. Sie müssen diese serverseitigen Endpunkte erstellen, um Challenges zu generieren und Lösungen zu validieren. Siehe die [Next.js-Server-Anleitung](/de/server/nextjs) für die vollständige Route-Handler-Implementierung.

## Wichtige Hinweise

- **Verwenden Sie immer `'use client'`** — `RibauntWidget` wird im Browser gerendert und ist auf Browser-APIs angewiesen.
- **Verwenden Sie `next/dynamic` nicht** — der Wrapper übernimmt das dynamische Importieren selbst; das Hinzufügen von `next/dynamic` ist redundant.
- **Geben Sie `RIBAUNT_SECRET` nicht über `NEXT_PUBLIC_` preis** — das Geheimnis muss ausschließlich serverseitig bleiben.
- **Verwenden Sie HTTPS oder localhost** — das Lösen im Browser hängt von der Web Crypto API ab, die auf sichere Kontexte beschränkt ist. Einfache lokale Netzwerk-HTTP-URLs (z. B. `http://192.168.x.x`) funktionieren nicht.
- **Für Serverless- oder Edge-Bereitstellungen** — der Standard-Replay-Schutz verwendet prozesslokalen Speicher, der nicht über Serverless-Instanzen hinweg geteilt wird. Setzen Sie `replayPrevention: 'remote'` mit einem atomaren verteilten Store, um Replay-Angriffe in solchen Umgebungen zu verhindern.
